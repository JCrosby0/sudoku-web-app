describe("Regions", () => {
  it("Create New Region", () => {
    cy.visit("/");
    // open control tab
    // select region type
    // select cells
    // create region
  });
  it("Update a Region", () => {
    cy.visit("/");
    // select region from list
    // change region type
    // select cells
    // update region
  });
  it("Delete a Region", () => {
    cy.visit("/");
    // select region from list
    // delete region
  });
});
